// Animation transition constants for the enemy list
export const TOGGLE_TRANSITION = {
    duration: 0.2,
    ease: [0.4, 0, 0.2, 1] as const,
};

export const CONTAINER_TRANSITION = {
    duration: 0.2,
    ease: [0.4, 0, 0.2, 1] as const,
};
